---
platform: linux
image_resource:
  type: docker-image
  source:
    repository: 18fgsa/concourse-task

inputs:
- name: pipeline-tasks
- name: manifests
- name: certificates
outputs:
- name: bosh-errand

run:
  path: pipeline-tasks/update-runtime-config.sh

params:
  BOSH_ENV:
  BOSH_CACERT:
  BOSH_USERNAME:
  BOSH_PASSWORD:
