---
platform: linux
image_resource:
  type: docker-image
  source:
    repository: 18fgsa/concourse-task

inputs:
  - name: pipeline-tasks
  - name: terraform-state
  - name: terraform-templates
outputs:
  - name: terraform-result

run:
  path: pipeline-tasks/terraform-apply.sh

params:
  STATE_FILE:
  TF_VAR_ACCESS_KEY_ID:
  TF_VAR_SECRET_ACCESS_KEY:
  TF_VAR_DEFAULT_REGION: