---
platform: linux
image_resource:
  type: docker-image
  source:
    repository: 18fgsa/concourse-task

inputs:
  - name: pipelines
outputs:
  - name: diff-templates
run:
  path: pipelines/tasks/diff-templates.sh

params:
  TEMPLATES_DIR: